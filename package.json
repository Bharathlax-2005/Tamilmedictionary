{
  "name": "tamilmedictionary-root",
  "private": true,
  "scripts": {
    "postinstall": "npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend"
  }
}
